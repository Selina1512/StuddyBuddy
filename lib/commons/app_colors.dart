import 'package:flutter/material.dart';

/// A class that contains the colors used in the app.
class AppColors {
  /// The primary color of the app.
  static const Color primaryColor = Colors.blue;
  /// The secondary color of the app.
  static const Color white = Colors.white;
  /// The tertiary color of the app.
  static Color? tertiaryColor = Colors.grey[800];
  /// The color of the app's background.
  static Color tabBackgroundColor = Colors.blue.withOpacity(0.3);
}
